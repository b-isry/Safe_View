// SafeView — demoMode.ts
// Purpose: Demo classifier switch — each frame toggles full-video blur ON/OFF.

/** When true, nudity classifier drives full-video blur with hysteresis + hold smoothing. */
export const DEMO_CLASSIFIER_SWITCH = true;
